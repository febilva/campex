var new_course_row = '<tr>' +
  '<td><%= "#{@paper_assignment.batch.code}"  %></td>' +
  '<%= "<td> #{@paper_assignment.paper.name} </td>".html_safe %>' +
  '<%= "<td> #{@paper_assignment.paper.code} </td>".html_safe %>' +
  '<td class="center aligned">' +
  '<%= link_to 'Remove', @paper_assignment, remote: true, method: :delete, data: { confirm: 'Are you sure?' } %>' +
  '</td>' +
  '</tr>';

$('#programmes_table').find('tbody').append(new_course_row);