<%= form_for(@term_structure_entry) do |f| %>
  <% if @term_structure_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@term_structure_entry.errors.count, "error") %> prohibited this term_structure_entry from being saved:</h2>

      <ul>
      <% @term_structure_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :term_structure_id %><br>
    <%= f.text_field :term_structure_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_field :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
